import styles from './ImageEditor.styles';
/**
 * Component provides text instruction for Image Editor
 */
const Instruction = () => {
    return <p style={styles.instruction}>Drag, Zoom, or Rotate image</p>;
};

export default Instruction;
